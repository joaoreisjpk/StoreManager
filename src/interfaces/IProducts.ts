interface basicProduct {
  name: string;
  quantity: number;
}

/* interface noNameProduct {
  quantity: number
}

interface noQuantityProduct {
  name: string;
} */

interface basicProductResponse {
  id: number;
  name: string;
  quantity: number;
}

